/**
 * useAdminSupportNotifications Hook
 * ⚠️ TICKETING SYSTEM MIGRATED TO TAWK.TO
 * This hook has been disabled. Original implementation preserved in git history.
 */

'use client';

export function useAdminSupportNotifications() {
  return {
    unreadCount: 0,
    hasUnread: false,
  };
}

/* ============================================================================
 * ORIGINAL IMPLEMENTATION (COMMENTED OUT - MIGRATED TO TAWK.TO)
 * ============================================================================
 *
 * This hook provided real-time notification counts for unread support messages
 * in the admin sidebar. Features included:
 * - Real-time unread message counting
 * - Automatic badge updates
 * - Query invalidation on message changes
 *
 * See git history to restore the full implementation.
 *
 * ============================================================================ */
